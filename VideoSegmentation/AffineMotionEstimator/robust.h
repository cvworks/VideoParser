# define        DIM             512
# define        DIMH            1024
# define        FILTER          100
# define        PI              3.141592654
# define        ROUND(A)        ((A)<0.0?-((int)(-(A)+0.5)):((int)((A)+0.5)))
# define        max(A,B)        ((A)<(B)?(B):(A))
# define        min(A,B)        ((A)<(B)?(A):(B))
# define        sign(A,B)        ((A)<0?-(B):(B))
# define        abs(A)        ((A)<0?-(A):(A))

#define ROOT2 1.4142136
#define MAXLEN 256

#define STOP_THRESHOLD 0.000001
#define AFFINE_THRESHOLD 0.00001
#define MIN_AFFINE_SIZE 50
#define MIN_PLANAR_SIZE 100

